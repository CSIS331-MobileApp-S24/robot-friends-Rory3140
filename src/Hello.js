
const Hello = ({greeting}) => {
  return (
    <div>
      <h1 className='tc'>Hello There!</h1>
      <p>{greeting}</p>
      <p>Wlecome to mobile app develompent</p>
    </div>
  );
};

export default Hello;
